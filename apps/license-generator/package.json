{
  "name": "@easysignage/license-generator",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.preload.json && node -e \"require('fs').cpSync('src/ui','dist/ui',{recursive:true})\"",
    "start": "electron .",
    "dev": "pnpm build && electron .",
    "lint": "echo \"lint pending\""
  },
  "dependencies": {
    "@easysignage/license-core": "workspace:*"
  },
  "devDependencies": {
    "electron": "^33.3.1",
    "typescript": "^5.7.2"
  }
}
