{
  "name": "@easysignage/electron-player",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json",
    "dev": "echo \"Electron dev: configurar electron + vite renderer na próxima iteração\"",
    "test": "vitest run",
    "lint": "echo \"lint pending\""
  },
  "dependencies": {
    "@easysignage/device-protocol": "workspace:*",
    "@easysignage/shared-types": "workspace:*",
    "electron-updater": "^6.8.9"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.3.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
