{
  "name": "@easysignage/webos-player",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper webOS TV (kiosk WebView) para o apps/web-player — PR 7.2.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "package": "ares-package . -o dist"
  },
  "devDependencies": {
    "@webosose/ares-cli": "^3.1.0"
  }
}
